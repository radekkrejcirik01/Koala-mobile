import { StyleSheet } from 'react-native';

export const ChatListStyle = StyleSheet.create({
  container: {
    flex: 1
  },
  scrollViewContainer: {
    paddingLeft: 12,
    paddingRight: 10
  }
});
