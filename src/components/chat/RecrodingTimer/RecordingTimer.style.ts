import { StyleSheet } from 'react-native';
import COLORS from '@constants/COLORS';

export const RecordingTimerStyle = StyleSheet.create({
    view: {
        marginLeft: 5,
        minWidth: 50,
        alignItems: 'center'
    },
    text: {
        color: COLORS.BLACK
    }
});
