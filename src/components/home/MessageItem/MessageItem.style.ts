import { StyleSheet } from 'react-native';

export const MessageItemStyle = StyleSheet.create({
  buttonView: {
    padding: 15,
    flexDirection: 'row'
  },
  messageText: {
    flex: 1,
    fontSize: 16,
    fontWeight: '500'
  }
});
