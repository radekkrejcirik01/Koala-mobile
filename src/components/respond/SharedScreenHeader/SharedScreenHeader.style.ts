import { StyleSheet } from 'react-native';

export const SharedScreenHeaderStyle = StyleSheet.create({
    container: {
        flexDirection: 'row',
        alignItems: 'center'
    },
    titleText: {
        marginLeft: 10,
        fontSize: 24,
        fontWeight: '600'
    }
});
