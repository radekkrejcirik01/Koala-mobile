import { StyleSheet } from 'react-native';

export const HomeScreenStyle = StyleSheet.create({
  scrollView: {
    flex: 1
  },
  scrollViewContainer: {
    paddingTop: 10,
    paddingBottom: 100
  }
});
