import { StyleSheet } from 'react-native';

export const NotificationsScreenStyle = StyleSheet.create({
    container: {
        flex: 1
    },
    listContainer: {
        paddingBottom: 50
    },
    listEmptyText: {
        marginTop: 100,
        fontSize: 16,
        alignSelf: 'center',
        fontWeight: '500'
    }
});
