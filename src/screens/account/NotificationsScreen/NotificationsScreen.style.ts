import { StyleSheet } from 'react-native';

export const NotificationsScreenStyle = StyleSheet.create({
    container: {
        flex: 1
    },
    listContainer: {
        paddingBottom: 50
    }
});
